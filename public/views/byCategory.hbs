<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="autor" content="Lautaro Santillan">
    <title>By Category - Proyecto Final Backend</title>
    <link 
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" 
        rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" 
        crossorigin="anonymous"
    >
    <link rel="icon" sizes="16x16" href="https://cdn3.iconfinder.com/data/icons/luchesa-vol-9/128/Html-128.png">
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>
    <link rel="stylesheet" href="../../css/style.css">
</head>
<body>
    <header>
        {{> navbar}}
    </header>
    <main class="container-fluid">
        <div class="col my-4">
            <h4 class="text-primary h4 text-center">Categoría {{category}}</h4>
            <form action="/productos/view/:category" style="text-align: center; margin-top: 15px;">
                <select name="category" id="category">
                    <option value="lego">Lego</option>
                    <option value="deportes">Deportes</option>
                    <option value="accion">Acción</option>
                </select>
                <button type="submit" class="btn btn-dark">Ver</button>
            </form>
            <div id="list" class="section__tablaProductos">
                {{#if productos }}
                    <h2 class="text-primary mt-4 text-center">Listado de Productos</h2>
                    <div class="container-fluid my-4 flex__center cards">
                        {{#each productos}}
                            <div class="col container my-4 card">
                                <h4 class="text-primary h4 text-center">{{title}}</h4>
                                <img src="{{thumbnail}}" class="img-fluid img__card"/>
                                <p class="text-center text-primary">Precio: ${{price}}</p>
                                <form action='/cart' method='POST' {{!-- onsubmit="return agregarProducto(this)" --}}>
                                    <input type='hidden' name='productId' value='{{_id}}' id="productId" /> {{!-- lo recibimos al post en /cart con req.body.productId --}}
                                    <button type='submit' class='btn btn-primary'>Agregar al carrito</button>
                                </form>
                                <form action='/productos/{{_id}}' method='GET' {{!-- onsubmit="return agregarProducto(this)" --}}>
                                    <button type='submit' class='btn btn-primary'>Ver más detalles</button>
                                </form>
                            </div>
                        {{/each}}
                    </div>
                    {{else}}
                        <h3 class="alert-danger rounded p-3 section">No se encontraron productos</h3>  
                {{/if}}
            </div>
        </div>
    </main>
    <footer>
        {{> footer}}
    </footer>
    <script 
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" 
        integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" 
        crossorigin="anonymous">
    </script>
    <script src="https://cdn.jsdelivr.net/npm/normalizr@3.6.1/dist/normalizr.browser.min.js"></script>
</body>
</html>